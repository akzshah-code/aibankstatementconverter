// FIX: The reference to "vite/client" was removed to resolve a "Cannot find type definition file" error.
// This is often caused by a tsconfig.json misconfiguration. Since no Vite-specific client types
// (like import.meta.env) are used in the codebase, removing this line is a safe workaround.
